name: Employee Management CI

on:
  push:
    paths: ['employee-management/**', '.github/workflows/employee-management.yml']
  pull_request:
    paths: ['employee-management/**', '.github/workflows/employee-management.yml']

jobs:
  server:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ems
          POSTGRES_PASSWORD: ems
          POSTGRES_DB: ems_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    defaults:
      run:
        working-directory: employee-management/server
    env:
      TEST_DATABASE_URL: postgresql://ems:ems@localhost:5432/ems_test
      JWT_SECRET: ci_secret_value_for_tests_only
      TZ: Africa/Dakar
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: employee-management/server/package-lock.json
      - run: npm ci
      - run: npm test

  client:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: employee-management/client
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: employee-management/client/package-lock.json
      - run: npm ci
      - run: npm run build
